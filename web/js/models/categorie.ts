export enum batimentCategorie {
    VIDE = 0,
    MAISON = 1,
    ARBRE = 2
}

export enum imageCategorie {
    BATIMENT = "batiment",
    VEGETATION = "vegetation",
    POLUTION = "polution"
}